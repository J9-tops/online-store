model Sale {
    id             String  @id @default(cuid())
    title          String
    description    String
    discountBadge  String
    discountAmount Float
    couponCode     String
    validFrom      String
    validUntil     String
    isActive       Boolean
    imageUrl       String

    @@unique([title, description])
    @@index([title])
}
